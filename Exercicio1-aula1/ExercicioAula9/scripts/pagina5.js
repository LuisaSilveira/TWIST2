/*Desenvolva um pagina que deve calcular o fatorial de um numero N. Regra:
O usuário deve informar o número de entrada por mensagem;
N deve ser número inteiro positivo acima de 1;
Exiba esse resultado por mensagens.
*/

let num = parseInt(prompt("Digite um numero inteiro: "));
if(num>1 && typeof(num)=== int){
    let n =1;
    while(n!=(num+1)){
        n*
    }
}